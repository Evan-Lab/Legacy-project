{# 
  Generated by JinjaGenerator for template 'templm/ancsosa_tg'
    at 2025-10-26T15:16:19.787282

  Used macros:
  - one_ancestor()
  - one_child()
  - other_family(z1)
  - ancestor_families()
  - tGup()
  - tG()

  Used includes:
  - ancsosa_deslist

  Used variables:
  - child.sosa_in_list (x1)
  - child.title (x1)
  - child.dates (x1)
  - l1 (x4)
  - evar.only (x2)
  - evar.spouse (x2)
  - evar.siblings (x1)
  - level (x1)
  - lchild (x2)
  - sp (x1)
  - ancestor.nb_families (x1)
  - ancestor.anc_sosa.v (x1)
  - pvar[1] (x1)
  - pvar[1].dates (x1)
  - pvar[1].mark_descendants (x1)
  - reset_count (x2)
  - l_up (x1)
#}
<!-- $Id: ancsosa_tg.txt,v 7.00 2014/11/20 11:45:02 mr Exp $ -->
{% include 'ancsosa_deslist' %}
{% macro one_ancestor() %}
    {{ link1('ancestor', 'ancestor') }}{{ somebody_info1("ancestor") }}{{ somebody_info2("ancestor") }}
{% endmacro %}
{% macro one_child() %}
    {% with %}
        {% set l1 = child.sosa_in_list %}
        {% if ( ( l1 != "" ) and ( ( evar.only != "on" ) and ( level != 2 ) ) ) %}
            {{ link1("child", lchild) }}{{ child.title }}{{ child.dates }}{{ sp }}(&rArr;
            <strong>
                <a href="#s{{ l1 }}" title="{{ _(see) }} {{ l1 }}">{{ l1 }}</a></strong>.)
                {% else %}
                    {{ link1("child", lchild) }}{{ somebody_info1("child") }}{{ somebody_info2("child") }}
                    {% if ( evar.spouse == "on" ) %}{# foreach not implemented yet #}{% endif %}
                {% endif %}
            {% endwith %}
        {% endmacro %}
        {% macro other_family(z1) %}
            {% if ( ( evar.spouse == "on" ) and ( ( evar.siblings == "on" ) and ( ancestor.nb_families > 1 ) ) ) %}
                <dl class="other_family">
                    <dd>
                        {# foreach not implemented yet #}
                    </dd>
                </dl>
            {% endif %}
        {% endmacro %}
        {% macro ancestor_families() %}
            {% if ( ( ancestor.anc_sosa.v % 2 ) == 0 ) %}
                {{ other_family("father") }}
            {% else %}
                {# foreach not implemented yet #}{{ other_family("mother") }}
            {% endif %}
        {% endmacro %}
        {% macro tGup() %}
            <p>{{ _(up to) |capitalize }} {{ pvar[1] }} {{ pvar[1].dates }}</p>
            {{ reset_count }}{{ pvar[1].mark_descendants }}{# foreach not implemented yet #}{% endmacro %}{% macro tG() %}{{ reset_count }}{% if ( evar.only != "on" ) %}{{ togen() }}{% endif %}{# foreach not implemented yet #}
        {% endmacro %}
        {% if ( l_up == 1 ) %}
            {{ tGup() }}
        {% else %}
            {{ tG() }}
        {% endif %}
