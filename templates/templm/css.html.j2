{# 
  Generated by JinjaGenerator for template 'templm/css'
    at 2025-10-26T15:16:20.733598

  Used macros:
  None

  Used includes:
  None

  Used variables:
  - etc_prefix (x1)
  - bvar.css (x2)
  - bvar.uppercase (x2)
  - bvar.particles (x2)
#}
<!-- $Id: templm/css.txt v7.0 2017/11/11 05:46:40 $ -->
<link rel="stylesheet"
      href="{{ etc_prefix }}templm/css{{ bvar.css }}.css"
      media="all"
      title="css{{ bvar.css }}" />
<script src={{ url_for('static/', path='templm/js.js') }}></script>
{% if ( bvar.uppercase == "yes" ) %}
    <script>
        <!--
        {
            %
            if (bvar.uppercase == "yes") %
        } {
            %
            if (bvar.particles != "") %
        }
        var p_a = new Array({
            {
                bvar.particles
            }
        });
        {
            %
            else %
        }
        var p_a = new Array("AF", "AV", "D", "DAL", "DE", "DEN", "DES", "DI", "DU", "OF", "UND", "VAN", "VON", "ZU", "ZUR");
        {
            % endif %
        } {
            % endif %
        } -->
    </script>
<script src={{ url_for('static/', path='templm/js_uppercase.js') }}></script>
{% else %}
<script src={{ url_for('static/', path='templm/js_uppercase_no.js') }}></script>
{% endif %}
