{# 
  Generated by JinjaGenerator for template 'templm/updmenu'
    at 2025-10-26T15:16:38.143896

  Used macros:
  - jq1()
  - jq1a(z1, z2)
  - jq1b()

  Used includes:
  - upddag
  - updmenu1

  Used variables:
  - has_birth_date (x1)
  - slash_birth_date (x1)
  - has_baptism_date (x1)
  - slash_baptism_date (x1)
  - has_death_date (x1)
  - slash_death_date (x1)
  - has_burial_date (x1)
  - slash_burial_date (x1)
  - has_cremation_date (x1)
  - slash_cremation_date (x1)
  - first_name (x2)
  - surname (x2)
  - index (x2)
  - occ (x1)
  - sex (x1)
  - z1.has_birth_place (x1)
  - z1.birth_place (x1)
  - z1.has_baptism_place (x1)
  - z1.baptism_place (x1)
  - z1.is_dead (x2)
  - z1.has_death_place (x1)
  - z1.death_place (x1)
  - z1.has_burial_place (x1)
  - z1.burial_place (x1)
  - z1.has_cremation_place (x1)
  - z1.cremation_place (x1)
  - z1.has_birth_date (x1)
  - z1.slash_birth_date (x1)
  - z1.has_baptism_date (x1)
  - z1.slash_baptism_date (x1)
  - z1.has_death_date (x1)
  - z1.slash_death_date (x1)
  - z1.has_burial_date (x1)
  - z1.slash_burial_date (x1)
  - z1.has_cremation_date (x1)
  - z1.slash_cremation_date (x1)
  - z1.occupation (x1)
  - z1.first_name (x2)
  - z1.surname (x2)
  - z1.index (x1)
  - z1.occ (x1)
  - z1.sex (x1)
  - lbp (x2)
  - ldp (x2)
  - has_families (x1)
  - evar.jq1 (x1)
  - evar.jq1a (x1)
  - evar.jq1b (x1)
  - evar.upddag (x1)
#}
{% macro jq1() %}
    <div id="jql">
        {% if has_birth_date %}
             {{ slash_birth_date }}
        {% else %}
            {% if has_baptism_date %}{{ slash_baptism_date }}{% endif %}
        {% endif %}
         - 
        {% if has_death_date %}
            {{ slash_death_date }}
        {% else %}
            {% if has_burial_date %}
                {{ slash_burial_date }}
            {% else %}
                {% if has_cremation_date %}{{ slash_cremation_date }}{% endif %}
            {% endif %}
        {% endif %}
    </div>
    <div id="jq7">
        {% if ( ( first_name == "?" ) or ( surname == "?" ) ) %}
            {{ index }}
        {% else %}
            {{ occ }}
        {% endif %}
    </div>
    <div id="jq8">{{ first_name }}</div>
    <div id="jq9">{{ surname }}</div>
    <div id="jq10">{{ index }}</div>
    <div id="jq11">{{ sex }}</div>
{% endmacro %}
{% macro jq1a(z1, z2) %}
    {% with %}
        {% set lbp = (z1.birth_place) if (z1.has_birth_place) else ((z1.baptism_place) if (z1.has_baptism_place)) %}
        {% with %}
            {% set ldp = ((z1.death_place) if (z1.has_death_place) else ((z1.burial_place) if (z1.has_burial_place) else ((z1.cremation_place) if (z1.has_cremation_place)))) if (z1.is_dead) %}
            <div id="jql"></div>
            <div id="z2jq2">
                {% if z1.has_birth_date %}
                     {{ z1.slash_birth_date }}
                {% else %}
                    {% if z1.has_baptism_date %}{{ z1.slash_baptism_date }}{% endif %}
                {% endif %}
                 · {{ lbp }}
            </div>
            <div id="z2jq3">
                {% if z1.is_dead %}
                    {% if z1.has_death_date %}
                         {{ z1.slash_death_date }}
                    {% else %}
                        {% if z1.has_burial_date %}
                             {{ z1.slash_burial_date }}
                        {% else %}
                            {% if z1.has_cremation_date %}{{ z1.slash_cremation_date }}{% endif %}
                        {% endif %}
                    {% endif %}
                     · {{ ldp }}
                {% endif %}
            </div>
            <div id="z2jq4">{{ z1.occupation }}</div>
            <div id="z2jq5">{{ lbp }}</div>
            <div id="z2jq6">{{ ldp }}</div>
            <div id="z2jq7">
                {% if ( ( z1.first_name == "?" ) or ( z1.surname == "?" ) ) %}
                    {{ z1.index }}
                {% else %}
                    {{ z1.occ }}
                {% endif %}
            </div>
            <div id="z2jq8">{{ z1.first_name }}</div>
            <div id="z2jq9">{{ z1.surname }}</div>
            <div id="z2jq11">{{ z1.sex }}</div>
        {% endwith %}
    {% endwith %}
{% endmacro %}
{% macro jq1b() %}
    {{ jq1a('self', 'pa1') }}
    {% if has_families %}{# foreach not implemented yet #}{% endif %}
{% endmacro %}
{% if ( evar.jq1 == "on" ) %}
    {{ jq1() }}
{% else %}
    {% if ( evar.jq1a == "on" ) %}
        {{ jq1a('self', '') }}
    {% else %}
        {% if ( evar.jq1b == "on" ) %}
            {{ jq1b() }}
        {% else %}
            {% if ( evar.upddag == "on" ) %}
                {% include 'upddag' %}
            {% else %}
                {% include 'updmenu1' %}
            {% endif %}
        {% endif %}
    {% endif %}
{% endif %}
