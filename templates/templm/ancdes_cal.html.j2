{# 
  Generated by JinjaGenerator for template 'templm/ancdes_cal'
    at 2025-10-26T15:16:14.678666

  Used macros:
  - color(xx)
  - anc_cal()
  - des_cal(curlev, maxlev)
  - cal(z1)

  Used includes:
  None

  Used variables:
  - reset_count (x2)
  - empty_sorted_list (x1)
  - birth_date.day (x3)
  - birth_date.month (x2)
  - incr_count (x2)
  - mm_dd (x2)
  - index (x2)
  - birth_symbol (x1)
  - death_date.day (x3)
  - death_date.month (x2)
  - death_symbol (x1)
  - l_v (x1)
  - i (x1)
  - evar.m (x1)
#}
<!-- $Id: templm/ancdes_cal.txt v7.0 2014/11/25 21:14:52 $ -->
{% macro color(xx) %}
    {{ nth('/666/933/393/339/999/399/939/993/AAA/FAA/AFA/AAF/FFF/AFF/FAF/FFA', 'xx') }}
{% endmacro %}
{% macro anc_cal() %}
    {{ reset_count }}{{ empty_sorted_list }}{# foreach not implemented yet #}{% endmacro %}{% macro des_cal(curlev, maxlev) %}{% if ( birth_date.day != "" ) %}{{ incr_count }}{% with %}{% set mm_dd = ( birth_date.month + 100 )"_"( birth_date.day + 100 ) %}{{ add_in_sorted_list(mm_dd, birth_date.month, birth_date.day, index, birth_symbol) }}{% endwith %}{% endif %}{% if ( death_date.day != "" ) %}{{ incr_count }}{% with %}{% set mm_dd = ( death_date.month + 100 )"_"( death_date.day + 100 ) %}{{ add_in_sorted_list(mm_dd, death_date.month, death_date.day, index, death_symbol) }}{% endwith %}{% endif %}{# foreach not implemented yet #}
{% endmacro %}
{% macro cal(z1) %}
    {% if ( "z1" == "anc" ) %}
        {{ anc_cal() }}
    {% else %}
        {{ des_cal(0, l_v) }}
    {% endif %}
    <div class="stat_cal bcbg1">
        <span class="title" style="color:#EEE;">{{ _(calendar/calendars, variant='0') |capitalize }}</span>
        <div class="row">
            {% for i in 1..32 %}<div>{{ i }}</div>{% endfor %}
        </div>
        <div class="col">
            {{ _((month) , variant='0') }}
            <br>
            {{ _((month) , variant='1') }}
            <br>
            {{ _((month) , variant='2') }}
            <br>
            {{ _((month) , variant='3') }}
            <br>
            {{ _((month) , variant='4') }}
            <br>
            {{ _((month) , variant='5') }}
            <br>
            {{ _((month) , variant='6') }}
            <br>
            {{ _((month) , variant='7') }}
            <br>
            {{ _((month) , variant='8') }}
            <br>
            {{ _((month) , variant='9') }}
            <br>
            {{ _((month) , variant='10') }}
            <br>
            {{ _((month) , variant='11') }}
        </div>
        <br>
        <div class="body">{{ reset_count }}{# foreach not implemented yet #}</div>
    </div>
{% endmacro %}
{{ togen() }}
{% if ( evar.m == "A" ) %}
    {{ cal("anc") }}
{% else %}
    {{ cal("des") }}
{% endif %}
