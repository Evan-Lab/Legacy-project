{# 
  Generated by JinjaGenerator for template 'family'
    at 2025-10-29T22:35:01.658215

  Used macros:
  None

  Used includes:
  - css
  - hed
  - perso_utils
  - menubar
  - modules/arbre_famille
  - trl
  - copyr
  - js

  Used variables:
  - bvar.default_lang (x2)
  - evar.lang (x3)
  - evar.wide (x1)
  - has_public_name (x1)
  - has_qualifiers (x2)
  - public_name (x2)
  - qualifier (x2)
  - surname (x2)
  - first_name (x2)
  - body_prop (x1)
  - message_to_wizard (x1)
  - cancel_links (x1)
  - query_time (x1)
#}

<!DOCTYPE html>
<html lang="{% if bvar.default_lang != "" and evar.lang == "" %}{{ bvar.default_lang }}{% else %}{% if evar.lang != "" %}{{ evar.lang }}{% else %}en{% endif %}{% endif %}">
    <head>
        <!-- $Id: family.txt v7.1 30/10/2017 13:28:29 -->
        <title>
            {% if has_public_name %}
                {% if has_qualifiers %}
                    {{ public_name }} {{ qualifier }}
                {% else %}
                    {{ public_name }} {{ surname }}
                {% endif %}
            {% else %}
                {% if has_qualifiers %}
                    {{ first_name }} {{ qualifier }}
                {% else %}
                    {{ first_name }} {{ surname }}
                {% endif %}
            {% endif %}
        </title>
        <meta name="robots" content="none">
        <meta charset="UTF-8">
        <meta name="viewport"
              content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <link rel="shortcut icon"
              href="{{ url_for("static", path="images/favicon_gwd.png") }}">
        <link rel="apple-touch-icon"
              href="{{ url_for("static", path="images/favicon_gwd.png") }}">
        {% include 'css.html.j2' %}
    </head>
    <body {{ body_prop }}>
        {% include 'hed.html.j2' %}
        {% include 'perso_utils.html.j2' %}
        <a href="#content" class="sr-only sr-only-focusable">Skip navigation menu</a>
        <div class="container{% if evar.wide == "on" %}-fluid{% endif %}">
            {% include 'menubar.html.j2' %}
            {{ message_to_wizard }}
            <div id="content" tabindex="-1">{% include 'modules/arbre_famille.html.j2' %}</div>
            {% if not cancel_links %}
                {% include 'trl.html.j2' %}
                {% include 'copyr.html.j2' %}
                {{ query_time }}
            {% endif %}
        </div>
        {% include 'js.html.j2' %}
    </body>
</html>
