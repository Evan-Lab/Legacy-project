{# 
  Generated by JinjaGenerator for template 'moved'
    at 2025-10-29T22:35:04.900943

  Used macros:
  None

  Used includes:
  - css
  - js
  - trl
  - copyr

  Used variables:
  - lang (x1)
  - bname (x2)
  - bvar.moved (x4)
  - query_time (x1)
#}

<!DOCTYPE html>
<html lang="{{ lang }}">
    <head>
        <title>{{ bname }} -&gt; {{ bvar.moved }}</title>
        <meta name="robots" content="none">
        <meta charset="UTF-8">
        <meta name="viewport"
              content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <link rel="shortcut icon"
              href="{{ url_for("static", path="images/favicon_gwd.png") }}">
        {% include 'css.html.j2' %}
        {% include 'js.html.j2' %}
    </head>
    <body>
        <div class="container">
            <h1>{{ bname }} -&gt; {{ bvar.moved }}</h1>
            <p>
                {{ interp(_('
                                en: The database "%bname;" has moved to:
                                fr: La base de données "%bname;" se trouve maintenant à l\'adresse:
                                ') ) }}
                <dl>
                    <dt>
                        <dd>
                            <a href="{{ bvar.moved }}">{{ bvar.moved }}</a>
                        </dd>
                    </dt>
                </dl>
            </p>
            {% include 'trl.html.j2' %}
            {% include 'copyr.html.j2' %}
            {{ query_time }}
        </div>
    </body>
</html>
