{# 
  Generated by JinjaGenerator for template 'stats'
    at 2025-10-26T20:24:46.345297

  Used macros:
  - births(nnn)
  - deaths(nnn)
  - marriages(nnn)
  - engaged(nnn)
  - alive(nnn)
  - lived(nnn)

  Used includes:
  - css
  - hed
  - home
  - trl
  - copyr
  - js

  Used variables:
  - lang (x1)
  - body_prop (x1)
  - message_to_wizard (x1)
  - bvar.latest_event (x2)
  - wizard (x2)
  - friend (x2)
  - n (x12)
  - query_time (x1)
#}
<!DOCTYPE html>
<html lang="{{ lang }}">
    <head>
        <!-- $Id: stats.txt v7.1 27/10/2023 03:33:17 $ -->
        <!-- Copyright (c) 1998-2007 INRIA -->
        <title>{{ _("statistics") |capitalize }}</title>
        <meta name="robots" content="none">
        <meta charset="UTF-8">
        <meta name="viewport"
              content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <link rel="shortcut icon"
              href="{{ url_for('static', path='images/favicon_gwd.png') }}">
        {% include 'css.html.j2' %}
    </head>
    <body {{ body_prop }}>
        {% include 'hed.html.j2' %}
        {{ message_to_wizard }}
        <div class="container">
            {% include 'home.html.j2' %}
            <h1>{{ _("statistics") |capitalize }}</h1>
            {% macro births(nnn) %}
                {{ _("the latest %d births:::nnn") }}
            {% endmacro %}
            {% macro deaths(nnn) %}
                {{ _("the latest %t deaths:::nnn") }}
            {% endmacro %}
            {% macro marriages(nnn) %}
                {{ _("the latest %d marriages:::nnn") }}
            {% endmacro %}
            {% macro engaged(nnn) %}
                {{ _("the %d oldest couples perhaps still alive and engaged:::nnn") }}
            {% endmacro %}
            {% macro alive(nnn) %}
                {{ _("the %d oldest perhaps still alive:::nnn") }}
            {% endmacro %}
            {% macro lived(nnn) %}
                {{ _("the %d who lived the longest:::nnn") }}
            {% endmacro %}
            {% with %}
                {% set n = (bvar.latest_event) if (( bvar.latest_event != "" )) else ("20") %}
                <ul>
                    {% if ( wizard or friend ) %}
                        <li>
                            <a href="/m=LB&k={{ n }}">{{ births(n) }}</a>
                        </li>
                        <li>
                            <a href="/m=LD&k={{ n }}">{{ deaths(n) }}</a>
                        </li>
                        <li>
                            <a href="/m=LM&k={{ n }}">{{ marriages(n) }}</a>
                        </li>
                        <li>
                            <a href="/m=OE&k={{ n }}">{{ engaged(n) }}</a>
                        </li>
                        <li>
                            <a href="/m=OA&k={{ n }}&lim=0">{{ alive(n) }}</a>
                        </li>
                    {% endif %}
                    <li>
                        <a href="/m=LL&k={{ n }}">{{ lived(n) }}</a>
                    </li>
                </ul>
                {% if ( wizard or friend ) %}
                    <ul>
                        <li>
                            <a href="/m=POP_PYR">{{ _("population pyramid") }}</a>
                        </li>
                    </ul>
                {% endif %}
                {% include 'trl.html.j2' %}
                {% include 'copyr.html.j2' %}
                {{ query_time }}
            </div>
            {% include 'js.html.j2' %}
        </body>
    </html>
{% endwith %}
