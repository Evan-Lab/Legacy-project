{# 
  Generated by JinjaGenerator for template 'modules/notes'
    at 2025-10-26T15:16:13.452979

  Used macros:
  None

  Used includes:
  None

  Used variables:
  - op_m (x4)
  - has_families (x2)
  - else (x1)
  - reset_count (x3)
  - has_linked_page.BNOTE (x5)
  - has_linked_page.NOTE (x4)
  - incr_count (x7)
  - has_notes (x3)
  - has_birth_note (x2)
  - has_baptism_note (x2)
  - has_death_note (x2)
  - has_burial_note (x3)
  - reset_count2 (x1)
  - wizard (x1)
  - friend (x1)
  - bvar.no_note_for_visitor (x1)
  - count2 (x3)
  - count (x2)
  - linked_page.BNOTE (x5)
  - linked_page.NOTE (x4)
  - notes (x2)
  - birth_note (x1)
  - baptism_note (x1)
  - death_note (x1)
  - is_buried (x1)
  - burial_note (x2)
  - is_cremated (x1)
  - nl (x1)
#}
<!-- $Id: modules/notes.txt v7.1 15/12/2022 23:41:44 $ -->
{% with %}
    {% set op_m = (op_m) if (( op_m != "" )) else (("1") if (has_families)else"2") %}{{ reset_count }}
    {% if has_linked_page.BNOTE %}{{ incr_count }}{% endif %}
    {% if has_notes %}{{ incr_count }}{% endif %}
    {% if has_birth_note %}{{ incr_count }}{% endif %}
    {% if has_baptism_note %}{{ incr_count }}{% endif %}
    {% if has_death_note %}{{ incr_count }}{% endif %}
    {% if has_burial_note %}{{ incr_count }}{% endif %}
    {% if has_linked_page.NOTE %}{{ incr_count }}{% endif %}
    {# foreach not implemented yet #}{{ reset_count2 }}{# foreach not implemented yet #}
    {% if ( ( op_m == 1 ) and ( ( wizard or ( friend or ( bvar.no_note_for_visitor != "yes" ) ) ) and ( has_notes or ( has_linked_page.BNOTE or ( has_linked_page.NOTE or ( count2 > 0 ) ) ) ) ) ) %}
        {% if ( ( count > 0 ) or ( has_linked_page.BNOTE and ( linked_page.BNOTE != "" ) ) ) %}
            <h2 class="mt-2">{{ _(individual notes) |capitalize }}</h2>
            {% if ( has_linked_page.BNOTE and ( linked_page.BNOTE != "" ) ) %}
                <div class="ml-3">{{ capitalize(linked_page.BNOTE) }}</div>
            {% endif %}
            <div class="ml-3">{{ notes }}</div>
        {% endif %}
        {% if ( ( count2 > 0 ) and has_families ) %}{{ reset_count }}{# foreach not implemented yet #}{% endif %}
        {% if ( has_linked_page.NOTE and ( linked_page.NOTE != "" ) ) %}
            <div class="ml-3">{{ capitalize(linked_page.NOTE) }}</div>
        {% endif %}
    {% else %}
        {% if ( ( ( count > 0 ) or ( count2 > 0 ) ) and ( op_m == 2 ) ) %}
            <h3 id="notes" class="mt-2">{{ _(note/notes, variant='1') |capitalize }}</h3>
            {% if ( has_linked_page.BNOTE and ( linked_page.BNOTE != "" ) ) %}
                <blockquote>
                    {{ capitalize(linked_page.BNOTE) }}
                </blockquote>
            {% endif %}
            {% if has_notes %}
                <h2>{{ _(individual notes) |capitalize }}</h2>
                <blockquote>
                    {{ notes }}
                </blockquote>
            {% endif %}
            {% if has_birth_note %}
                <h3>{{ _(birth) |capitalize }}</h3>
                <blockquote>
                    {{ birth_note }}
                </blockquote>
            {% endif %}
            {% if has_baptism_note %}
                <h3>{{ _(baptism) |capitalize }}</h3>
                <blockquote>
                    {{ baptism_note }}
                </blockquote>
            {% endif %}
            {# foreach not implemented yet #}
            {% if has_death_note %}
                <h3>{{ _(death) |capitalize }}</h3>
                <blockquote>
                    {{ death_note }}
                </blockquote>
            {% endif %}
            {% if ( has_burial_note and is_buried ) %}
                <h3>{{ _(burial) |capitalize }}</h3>
                <blockquote>
                    {{ burial_note }}
                </blockquote>
            {% else %}
                {% if ( has_burial_note and is_cremated ) %}
                    <h3>{{ _(cremation) |capitalize }}</h3>
                    <blockquote>
                        {{ burial_note }}
                    </blockquote>
                {% endif %}
            {% endif %}
            {# foreach not implemented yet #}
            {% if ( has_linked_page.NOTE and ( linked_page.NOTE != "" ) ) %}
                <blockquote>
                    {{ capitalize(linked_page.NOTE) }}
                </blockquote>
            {% endif %}
            {{ nl }}
        {% endif %}
    {% endif %}
    {{ reset_count }}
{% endwith %}
