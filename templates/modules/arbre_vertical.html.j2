{# 
  Generated by JinjaGenerator for template 'modules/arbre_vertical'
    at 2025-10-26T15:16:12.025290

  Used macros:
  - a_tree_line()
  - line(xx, vv)

  Used includes:
  - modules/arbre_8gen
  - buttons_dag

  Used variables:
  - is_first (x1)
  - is_last (x1)
  - vv (x2)
  - xx.has_parents (x1)
  - xx.access (x1)
  - xx (x3)
  - xx.title (x1)
  - xx.index (x1)
  - xx.dates (x1)
  - xx.has_image (x1)
  - xx.image_html_url (x1)
  - xx.image_url (x1)
  - xx.image_small_size (x1)
  - e.t (x4)
  - e.im (x1)
  - e.v (x2)
  - e.t1 (x1)
  - cancel_links (x6)
  - self (x1)
  - nl (x1)
  - has_parents (x1)
  - e_v (x2)
#}
<!-- $Id: modules/arbre_vertical.txt v7.1 04/03/2023 10:39:16 $ -->
{% macro a_tree_line() %}
    {% if ( not is_first ) %}<tr>{# foreach not implemented yet #}</tr>{% endif %}
    <tr>{# foreach not implemented yet #}</tr>
    {% if ( not is_last ) %}
        <tr>{# foreach not implemented yet #}</tr>
        <tr>{# foreach not implemented yet #}</tr>
    {% endif %}
{% endmacro %}
{% macro line(xx, vv) %}
    {% if ( vv > 0 ) %}
        {% if xx.has_parents %}{{ line(("xx.father") if (( e.t == "A" )) else ("xx.mother"), ( vv - 1 )) }}{% endif %}
        {% if ( not cancel_links ) %}<a href="/{{ xx.access }}">{% endif %}
            {{ xx }}
            {% if ( not cancel_links ) %}</a>{% endif %}
        {{ xx.title }}
        {% if ( not cancel_links ) %}<a href="/{{ xx.index }}">{% endif %}
            <i>{{ xx.dates }}</i>
            {% if ( not cancel_links ) %}</a>{% endif %}
        {% if ( xx.has_image and ( e.im != 0 ) ) %}
            <div>
                {% if ( not cancel_links ) %}<a href="{{ xx.image_html_url }}">{% endif %}
                    <img class="rounded"
                         src="{{ xx.image_url }}"
                         {{ xx.image_small_size }}
                         alt="{{ _(image/images, variant='0') }} {{ xx }}"
                         title="{{ _(image/images, variant='0') }}">
                    {% if ( not cancel_links ) %}</a>{% endif %}
            </div>
        {% endif %}
        {% if ( xx != self ) %}<div>|</div>{% endif %}
    {% endif %}
    {{ nl }}
{% endmacro %}
{% with %}
    {% set e_v = (e.v) if (( e.v != "" )) else ("3") %}
    {% if has_parents %}
        {% if ( ( e.t == "T" ) and ( e.t1 == "m" ) ) %}
            <div class="row mx-auto mt-2">{% include 'modules/arbre_8gen' %}</div>
        {% else %}
            {% if ( ( e.t == "A" ) or ( e.t == "C" ) ) %}
                <div class="text-center col-6 mt-2">{{ line("self", ( e_v + 1) ) }}</div>
            {% else %}
                <div class="d-flex mt-2 mx-3{% if ( e_v < 3 ) %} justify-content-center col-6{% endif %}">
                    <table class="tree-vert">
                        {# foreach not implemented yet #}
                    </table>
                </div>
            {% endif %}
        {% endif %}
    {% endif %}
    {% include 'buttons_dag' %}
{% endwith %}
