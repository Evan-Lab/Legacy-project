{# 
  Generated by JinjaGenerator for template 'modules/gr_parents'
    at 2025-10-26T15:16:12.363674

  Used macros:
  None

  Used includes:
  None

  Used variables:
  - index (x1)
  - op_m (x2)
  - has_parents (x3)
  - father.has_parents (x7)
  - mother.has_parents (x7)
  - border (x1)
  - bi (x10)
  - has_siblings (x3)
#}
<!-- $Id: modules/gr_parents.txt v7.0 27/06/2018 18:47:46 $ -->
{% with %}
    {% set bi = index %}
    {% if ( ( op_m == 1 ) and has_parents ) %}
        {% if ( not ( father.has_parents and mother.has_parents ) ) %}
            {% if father.has_parents %}
                <h2>{{ _(paternal grand-parents) |capitalize }},&nbsp;{{ _(uncles and aunts, variant='1') }}</h2>
            {% else %}
                {% if mother.has_parents %}
                    <h2>{{ _(maternal grand-parents) |capitalize }},&nbsp;{{ _(uncles and aunts, variant='1') }}</h2>
                {% endif %}
            {% endif %}
        {% endif %}
        <table style="border:{{ border }};width:100%">
            <tr>
                {% if father.has_parents %}
                    <td style="vertical-align:top;text-align:left;width:50%">
                        {% if mother.has_parents %}
                            <h2>{{ _(paternal grand-parents) |capitalize }},&nbsp;{{ _(uncles and aunts, variant='1') }}</h2>
                        {% endif %}
                        <ul>
                            {{ descendancy_grand_fathers("father", "parhigh", bi) }}{{ descendancy_grand_mothers("father", "parhigh", bi) }}
                        </ul>
                    </td>
                {% endif %}
                <td style="vertical-align:top;text-align:left;width:50%">
                    {% if mother.has_parents %}
                        {% if father.has_parents %}
                            <h2>{{ _(maternal grand-parents) |capitalize }},&nbsp;{{ _(uncles and aunts, variant='1') }}</h2>
                        {% endif %}
                        <ul>
                            {{ descendancy_grand_fathers("mother", "parhigh", bi) }}{{ descendancy_grand_mothers("mother", "parhigh", bi) }}
                        </ul>
                    {% endif %}
                </td>
                {% if ( not mother.has_parents ) %}
                    <td style="vertical-align:top;text-align:left;width:50%"></td>
                {% endif %}
            </tr>
        </table>
    {% else %}
        {% if ( ( op_m == 2 ) and ( has_parents and ( father.has_parents or mother.has_parents ) ) ) %}
            <div class="row">
                {% if father.has_parents %}
                    <div class="col-{% if ( not has_siblings ) %}6{% else %}4{% endif %}">
                        <h2>{{ _(paternal grand-parents) |capitalize }},&nbsp;{{ _(uncles and aunts, variant='1') }}</h2>
                        <ul class="list-unstyled small">
                            {{ descendancy_grand_fathers("father", "parhigh", bi) }}{{ descendancy_grand_mothers("father", "parhigh", bi) }}
                        </ul>
                    </div>
                {% endif %}
                <div class="col-4 {% if ( not father.has_parents ) %}offset-4{% endif %}">
                    {% if ( has_parents and has_siblings ) %}
                        <h2>{{ _(parents) |capitalize }}, {{ _(full siblings) }}</h2>
                        <ul class="list-unstyled small">
                            {{ descendancy_grand_fathers("self", "parhigh", bi) }}{{ descendancy_grand_mothers("self", "parhigh", bi) }}
                        </ul>
                    {% endif %}
                </div>
                {% if mother.has_parents %}
                    <div class="col-{% if ( not has_siblings ) %}6{% else %}4{% endif %}">
                        <h2>{{ _(maternal grand-parents) |capitalize }},&nbsp;{{ _(uncles and aunts, variant='1') }}</h2>
                        <ul class="list-unstyled small">
                            {{ descendancy_grand_fathers("mother", "parhigh", bi) }}{{ descendancy_grand_mothers("mother", "parhigh", bi) }}
                        </ul>
                    </div>
                {% endif %}
            </div>
        {% endif %}
    {% endif %}
{% endwith %}
