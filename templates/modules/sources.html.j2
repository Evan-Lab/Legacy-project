{# 
  Generated by JinjaGenerator for template 'modules/sources'
    at 2025-10-26T15:16:13.670109

  Used macros:
  None

  Used includes:
  None

  Used variables:
  - op_m (x3)
  - has_linked_page.BIBLIO (x1)
  - linked_page.BIBLIO (x2)
  - reset_count (x2)
  - has_sources (x1)
  - incr_count (x1)
  - count (x2)
#}
<!-- $Id: modules/sources.txt v7.0 28/10/2018 23:10:24 $ -->
{% with %}
    {% set op_m = (op_m) if (( op_m != "" )) else ("1") %}
    {% if ( has_linked_page.BIBLIO and ( linked_page.BIBLIO != "" ) ) %}
        <h2>{{ _(bibliography) |capitalize }}</h2>
        <ul>
            <li>{{ capitalize(linked_page.BIBLIO) }}</li>
        </ul>
    {% endif %}
    {{ reset_count }}
    {% if has_sources %}{{ incr_count }}{% endif %}
    {# foreach not implemented yet #}
    {% if ( count > 0 ) %}
        <h2 class="mt-2">
            {% if ( count == 1 ) %}
                {{ _(source/sources, variant='0') |capitalize }}
            {% else %}
                {{ _(source/sources, variant='1') |capitalize }}
            {% endif %}
        </h2>
        <ul class="pl-4 pY-0">
            {# foreach not implemented yet #}{% if ( op_m == 2 ) %}{{ reset_count }}{# foreach not implemented yet #}
        {% endif %}
    </ul>
{% endif %}
{% endwith %}
