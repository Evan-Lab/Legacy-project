{# 
  Generated by JinjaGenerator for template 'templm/anclist_fam7_timeline'
    at 2025-10-26T20:25:17.226322

  Used macros:
  - fam7_somebody1(xx, yy, lexa, lexb, sss)
  - fam7_somebody(xx, lexa, lexb, sss)
  - fam7_cousin(curlev, maxlev, lexa, lexb, sss)
  - fam7_2nd_cousin(curlev, maxlev, lexa, lexb, sss)
  - fam7_children(curlev, maxlev, lexa, lexb, sss)
  - fam7_nephew(curlev, maxlev, lexa, lexb, sss)
  - fam7()

  Used includes:
  - raw include

  Used variables:
  - bvar.birth_symbol (x2)
  - evar.cousins (x1)
  - evar.spouse (x1)
  - evar.maxv (x1)
  - evar.v (x2)
  - evar.image (x2)
  - xx.is_male (x2)
  - xx.is_female (x2)
  - xx.birth_date.year (x3)
  - xx.birth_date.prec (x2)
  - xx.is_dead (x2)
  - xx.death_date.year (x3)
  - xx.death_date.prec (x2)
  - xx.computable_age (x2)
  - xx.birth_date.month (x3)
  - xx.birth_date.day (x3)
  - xx.sex (x2)
  - xx.is_restricted (x2)
  - xx (x2)
  - xx.access (x1)
  - xx.dates (x1)
  - xx.index (x4)
  - xx.occupation (x1)
  - xx.has_sosa (x2)
  - xx.has_families (x1)
  - xx.anc_sosa.v (x1)
  - xx.computable_death_age (x1)
  - xx.death_age (x1)
  - xx.age (x1)
  - sss (x2)
  - lxby (x21)
  - today.year (x2)
  - lby (x22)
  - lyo (x5)
  - lxdy (x8)
  - cancel_links (x3)
  - lurl (x1)
  - lxr (x4)
  - browsing_with_sosa_ref (x1)
  - l1i (x3)
  - sosa_ref.index (x1)
  - l1s (x1)
  - birth_symbol (x1)
  - lw2 (x4)
  - lw1 (x2)
  - nb_children (x4)
  - empty_sorted_list (x1)
  - birth_date.year (x4)
  - is_dead (x1)
  - death_date.year (x3)
  - ldy (x2)
  - computable_age (x1)
  - is_male (x1)
  - is_female (x1)
  - index (x2)
  - has_parents (x6)
  - father.index (x1)
  - father.has_parents (x2)
  - father.father.index (x1)
  - father.mother.index (x1)
  - mother.index (x1)
  - mother.has_parents (x2)
  - mother.father.index (x1)
  - mother.mother.index (x1)
  - reset_count (x2)
  - count (x3)
  - sp (x1)
  - incr_count (x1)
#}
<!-- $Id: templm/anclist_fam7_timeline.txt v7.0 2014/11/12 14:33:31 $ -->
{% with %}
    {% set birth_symbol = (bvar.birth_symbol) if (( bvar.birth_symbol != "" )) else ("&deg;") %}
    {% with %}
        {% set lurl = "/""m=A&t=F&fam7=on&tf1=fam73&cousins="evar.cousins"&spouse="evar.spouse"&maxv="evar.maxv"&v="evar.v"&" %}
        {% macro fam7_somebody1(xx, yy, lexa, lexb, sss) %}
            {% with %}
                {% set lxr = (a_of_b((_("lexa", variant='0')) if (xx.is_male) else ((_("lexa", variant='1')) if (xx.is_female) else (_("lexa", variant='2'))), (_("lexb", variant='0')) if (( sss == 0 )) else ((_("lexb", variant='1')) if (( sss == 1 )) else (_("lexb", variant='2'))))) if (( "lexb" != "" )) else ((_("lexa", variant='0')) if (xx.is_male) else ((_("lexa", variant='1')) if (xx.is_female) else (_("lexa", variant='2')))) %}
                {% with %}
                    {% set lxby = ((xx.birth_date.year) if (( xx.birth_date.year > 0 ))) if (( ( xx.birth_date.year != "" ) and ( ( xx.birth_date.prec != "|" ) and ( xx.birth_date.prec != ".." ) ) )) else ("9999") %}
                    {% with %}
                        {% set lxdy = (((xx.death_date.year) if (( xx.death_date.year > 0 ))) if (( xx.is_dead and ( ( xx.death_date.year != "" ) and ( ( xx.death_date.prec != "|" ) and ( xx.death_date.prec != ".." ) ) ) )) else ((today.year) if (xx.computable_age) else (lxby))) if (( lxby != 9999 )) else (lxby) %}{{ add_in_sorted_list(lxby(("00") if (( xx.birth_date.month == "" )) else (("0") if (( xx.birth_date.month < 10 )))xx.birth_date.month("00") if (( xx.birth_date.day == "" )) else (("0") if (( xx.birth_date.day < 10 )))xx.birth_date.day) if (( lxby != 9999 )) else ("9999"), ((("+"( ( lxby - lby ) - lyo )) if (( ( lxby - lby ) > lyo )) else (( lxby - lby ))) if (( lxby >= lby )) else (("-"( lby - lxby )) if (( lby > lxby )))) if (( ( lxby != 9999 ) and ( lby != 9999 ) )), ( ( lxdy - lxby ) + 1 ), ((("+"( ( lxdy - lby ) - lyo )) if (( ( lxdy - lby ) > lyo )) else (( lxdy - lby ))) if (( lxdy >= lby )) else (("-"( lby - lxdy )) if (( lby > lxdy )))) if (( xx.is_dead and ( ( lxdy != 9999 ) and ( lby != 9999 ) ) )), xx.sex, (xx) if (( cancel_links or xx.is_restricted )) else ("<a href=""lurlxx.access"">"xx"</a>"), xx.dates, (lxr) if (( cancel_links or xx.is_restricted )) else (("<a href=""
   "
   /
   "
   "
   m="RLM"
   et="S"
   spouse="on"
   image="evar.image"
   i1="l1i"
   i2="xx.index"
   t2="lxr"
   i3="sosa_ref.index"
   t3="sosa_1"
   i4="l1i"
   t4="l1s"
   ">"lxr"</a>") if (browsing_with_sosa_ref) else ("<a href=""
   "
   /
   "
   "
   em="R"
   ei="xx.index"
   spouse="on"
   et="S"
   image="evar.image"
   i="l1i"
   ">"lxr"</a>")), xx.occupation, xx.index, ("<div style="width:"( lby - lxby )"px"></div>") if (( ( lxby != 9999 ) and ( ( lby != 9999 ) and ( lby > lxby ) ) )), "."xx.has_sosa, ("fam7_him_her") if (( "lexa" == "him/her" )), "li_"xx.sex" li_"xx.has_familiesxx.has_sosa, ((xx.index) if (( ( "xx" != "ancestor" ) or ( ( xx.anc_sosa.v % 2 ) == 0 ) ))) if (( ( "xx" != "spouse" ) and ( "xx" != "parent" ) )), ("<span class="xs">"xx.death_age"</span>
                        ") if (xx.computable_death_age) else (("<span class="xs">"birth_symbol" "xx.age"</span>
                        ") if (xx.computable_age)), ((( lxby - lw2 )) if (( lxby >= lw2 )) else (("-"( lw2 - lxby )) if (( lw2 > lxby )))) if (( ( lxby != 9999 ) and ( lby != 9999 ) )) else ("0"), ("<div style="width:"lw1"px;"></div>") if (( "lexa" == "him/her" ))) }}
                    {% endwith %}
                {% endwith %}
            {% endwith %}
        {% endmacro %}
        {% macro fam7_somebody(xx, lexa, lexb, sss) %}
            {{ fam7_somebody1('xx', 'birth', 'lexa', 'lexb', 'sss') }}
        {% endmacro %}
        {% macro fam7_cousin(curlev, maxlev, lexa, lexb, sss) %}
            {% if ( nb_children > 0 ) %}{# foreach not implemented yet #}{% endif %}{% endmacro %}{% macro fam7_2nd_cousin(curlev, maxlev, lexa, lexb, sss) %}{% if ( nb_children > 0 ) %}{# foreach not implemented yet #}{% endif %}{% endmacro %}{% macro fam7_children(curlev, maxlev, lexa, lexb, sss) %}{% if ( nb_children > 0 ) %}{# foreach not implemented yet #}{% endif %}{% endmacro %}{% macro fam7_nephew(curlev, maxlev, lexa, lexb, sss) %}{% if ( nb_children > 0 ) %}{# foreach not implemented yet #}{% endif %}{% endmacro %}{% macro fam7() %}{% with %}{% set lv7 = min(evar.v, 4) %}{{ empty_sorted_list }}{# foreach not implemented yet #}
            {% endwith %}
        {% endmacro %}
        {% with %}
            {% set lby = (birth_date.year) if (( ( birth_date.year != "" ) and ( birth_date.year > 0 ) )) else ("9999") %}
            {% with %}
                {% set ldy = (death_date.year) if (( is_dead and ( ( death_date.year != "" ) and ( death_date.year > 0 ) ) )) else ("9999") %}
                {% with %}
                    {% set lyo = (( ldy - lby )) if (( ( lby != 9999 ) and ( ldy != 9999 ) )) else ("0") %}
                    {% with %}
                        {% set lw1 = (( today.year - birth_date.year )) if (computable_age) else (lyo) %}
                        {% with %}
                            {% set lw2 = (( ( lby + lw1 ) + 4 )) if (( lby != 9999 )) else ("0") %}
                            {% with %}
                                {% set l1s = (_("him/her", variant='0')) if (is_male) else ((_("him/her", variant='1')) if (is_female) else ("0")) %}
                                {% with %}
                                    {% set l1i = index %}
                                    {% with %}
                                        {% set l2i = (father.index) if (has_parents) %}
                                        {% with %}
                                            {% set l3i = (mother.index) if (has_parents) %}
                                            {% with %}
                                                {% set l4i = (father.father.index) if (( has_parents and father.has_parents )) %}
                                                {% with %}
                                                    {% set l5i = (father.mother.index) if (( has_parents and father.has_parents )) %}
                                                    {% with %}
                                                        {% set l6i = (mother.father.index) if (( has_parents and mother.has_parents )) %}
                                                        {% with %}
                                                            {% set l7i = (mother.mother.index) if (( has_parents and mother.has_parents )) %}{{ fam7() }}
                                                            <table summary="family life dates" class="fam7_table">
                                                                <colgroup>
                                                                    <col width="35" />
                                                                    <col width="80" />
                                                                    <col width="35" />
                                                                    <col width="1" />
                                                                    <col width="1" />
                                                                    <col width="1" />
                                                                    <col width="300" />
                                                                    <col width="150" />
                                                                    <col width="300" />
                                                                    <col width="300" />
                                                                </colgroup>
                                                                <thead>
                                                                    <tr>
                                                                        <th colspan="6">Χρόνος</th>
                                                                        <th colspan="2">{{ _("person/persons", variant='0') }}</th>
                                                                        <th>{{ _("relationship", variant='0') }}</th>
                                                                        <th>{{ _("occupation/occupations", variant='0') }}</th>
                                                                    </tr>
                                                                </thead>
                                                                <tbody>
                                                                    {{ reset_count }}{# foreach not implemented yet #}
                                                                    <tr>
                                                                        <td colspan="10">
                                                                            {{ _("total") |capitalize }} : {{ count }} [person/persons]1@(c)
                                                                            {{ sp }}
                                                                            {% if ( not cancel_links ) %}
                                                                                <div class="menu">
                                                                                    <span>
                                                                                        {{ reset_count }}<a title="{{ _("tree") }} (Y)"
   accesskey="Y"
   href="/m=RLM;et=S;spouse=on;{# foreach not implemented yet #}{{ incr_count }}i{{ count }}={{ index }}&t{{ count }}=*;">{{ _("tree") |capitalize }} 7 {{ _("family/families", variant='1') }}</a>
                                                                                    </span>
                                                                                </div>
                                                                            {% endif %}
                                                                        </td>
                                                                    </tr>
                                                                </tbody>
                                                            </table>
                                                        {% endwith %}
                                                    {% endwith %}
                                                {% endwith %}
                                            {% endwith %}
                                        {% endwith %}
                                    {% endwith %}
                                {% endwith %}
                            {% endwith %}
                        {% endwith %}
                    {% endwith %}
                {% endwith %}
            {% endwith %}
        {% endwith %}
    {% endwith %}
{% endwith %}
