{# 
  Generated by JinjaGenerator for template 'templm/ancsosa_same'
    at 2025-10-29T22:35:44.132263

  Used macros:
  - same()
  - same_consang()
  - same_same()

  Used includes:
  None

  Used variables:
  - ancestor.same (x2)
  - ancestor.sex (x1)
  - ancestor.anc_sosa (x1)
  - ancestor.access (x1)
  - ancestor (x2)
  - ancestor.dates (x1)
  - ancestor.index (x1)
  - index (x1)
  - self (x1)
  - reset_count (x2)
  - evar.same (x3)
  - count (x2)
  - bvar.show_consang (x1)
  - manitou (x1)
#}

<!-- $Id: templm/anclist_same.txt v7.0 2014/11/12 14:33:31 $ -->
{% macro same() %}
    {% if ancestor.same != "" %}
        <li class="li_{{ ancestor.sex }} li_10">
            {{ ancestor.anc_sosa }} -
            <a href="/{{ ancestor.access }}">{{ ancestor }}</a> {{ ancestor.dates }} ==
            <a href="/em=R&ei={{ index }}&i={{ ancestor.index }}&et=M&spouse=on"
               title="{{ ancestor }} …{{ _("relationship") }}… {{ self }}">{{ ancestor.same }}</a>
        </li>
    {% endif %}
{% endmacro %}
{% macro same_consang() %}
    {{ reset_count }}
    <p class="highlight">
        {{ _("display smallest sosa number relative to ancestor") |capitalize }} + {{ _("consanguinity") |capitalize }}
    </p>
    {{ togen() }}
    <ul>
        {# foreach not implemented yet #}
    </ul>
    <p>{{ _("total") |capitalize }} = {{ evar.same }} + {{ count }} = {{ evar.same + count }}</p>
{% endmacro %}
{% macro same_same() %}
    {{ reset_count }}
    <p class="highlight">{{ _("display smallest sosa number relative to ancestor") |capitalize }}</p>
    {{ togen() }}
    <ul>
        {# foreach not implemented yet #}
    </ul>
    <p>{{ _("total") |capitalize }} = {{ evar.same }}</p>
{% endmacro %}
{% if bvar.show_consang != "no" or manitou %}
    {{ same_consang() }}
{% else %}
    {{ same_same() }}
{% endif %}
